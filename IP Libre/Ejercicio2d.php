<?php

/**
 * Determina si una mascota es menor a la ingresada
 * @param array $unArregloMulti
 * @param int $unaEdad
 * @return float
 */
function buscarPrimerMenorA($unArregloMulti, $unaEdad){
    /*float $edadMenor*/
    $edadMenor = -1;
    if ($unaEdad > $edadMenor){
        $edadMenor = $unaEdad;
    }
return $edadMenor;
}